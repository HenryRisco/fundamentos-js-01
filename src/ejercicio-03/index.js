let numero = 3

const tipoNumero = numero > 0 ? 'positivo' : 'negativo o cero'

console.log(tipoNumero)

let cadena = 'Soy Henry'

const longitudCadena = cadena.length > 5 ? 'larga' : 'corta'

console.log(longitudCadena)
